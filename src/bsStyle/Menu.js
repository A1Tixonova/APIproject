import React, { useState } from "react";
import { Link } from "react-router";
import "./style.css";

const Menu = () => {
  return (
    <div className={"navbar-container"}>
      <ul className={"navlist-border"}>
        <a>First</a>
        <a>First</a>
        <a>First</a>
      </ul>
    </div>
  );
};

export default Menu;
